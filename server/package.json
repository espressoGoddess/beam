{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run migrate && NODE_ENV=development nodemon server.js",
    "start:prod": "npm run migrate:prod && NODE_ENV=production nodemon server.js",
    "migrate": "NODE_ENV=development knex migrate:latest",
    "migrate:prod": "NODE_ENV=production knex migrate:latest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "knex": "^2.4.2",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
